{
  "scripts": {
    "validate": "node validate.js",
    "check:content": "node scripts/i18n-check.js && node scripts/sync-scenario-md-dialogue.js --check",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "check": "npm run validate && npm run check:content && npm test",
    "check:all": "npm run check && npm run test:e2e"
  },
  "devDependencies": {
    "dotenv": "^17.3.1",
    "playwright": "^1.59.1",
    "sharp": "^0.35.3"
  }
}
